<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>信息</title>
    <link href="/2021/02/20/page/"/>
    <url>/2021/02/20/page/</url>
    
    <content type="html"><![CDATA[<p>史上最全 前端开发面试问题及答案整理 本文旨在加深对前端知识点的理解，资料来源于网络，由本人收集整理。</p><h2 id="优秀的面试文章："><a href="#优秀的面试文章：" class="headerlink" title="优秀的面试文章："></a>优秀的面试文章：</h2><ul><li>中高级前端大厂面试秘籍：<a href="https://juejin.im/post/5c64d15d6fb9a049d37f9c20">https://juejin.im/post/5c64d15d6fb9a049d37f9c20</a></li><li>前端面试查漏补缺：<a href="https://juejin.im/post/5c6bab91f265da2dd94c9f9e">https://juejin.im/post/5c6bab91f265da2dd94c9f9e</a></li><li>前端专题学习: <a href="https://github.com/mqyqingfeng/Blog">https://github.com/mqyqingfeng/Blog</a></li></ul><h2 id="前端开发面试知识点大纲："><a href="#前端开发面试知识点大纲：" class="headerlink" title="前端开发面试知识点大纲："></a>前端开发面试知识点大纲：</h2><pre><code>HTML&amp;CSS：  对Web标准的理解、浏览器内核差异、兼容性、hack、CSS基本功：布局、盒子模型、选择器优先级及使用、HTML5、CSS3、移动端适应。 JavaScript：   数据类型、面向对象、继承、闭包、插件、作用域、跨域、原型链、模块化、自定义事件、内存泄漏、事件机制、异步装载回调、模板引擎、Nodejs、JSON、ajax等。其他：  HTTP、安全、正则、优化、重构、响应式、移动端、团队协作、可维护、SEO、UED、架构、职业生涯 </code></pre><hr><h2 id="1-请你谈谈Cookie的弊端"><a href="#1-请你谈谈Cookie的弊端" class="headerlink" title="1.请你谈谈Cookie的弊端"></a>1.请你谈谈Cookie的弊端</h2><p><code>cookie</code>虽然在持久保存客户端数据提供了方便，分担了服务器存储的负担，但还是有很多局限性的。<br>第一：每个特定的域名下最多生成20个<code>cookie</code></p><pre><code>1.IE6或更低版本最多20个cookie2.IE7和之后的版本最后可以有50个cookie。3.Firefox最多50个cookie4.chrome和Safari没有做硬性限制</code></pre><p><code>IE</code>和<code>Opera</code> 会清理近期最少使用的<code>cookie</code>，<code>Firefox</code>会随机清理<code>cookie</code>。</p><p><code>cookie</code>的最大大约为<code>4096</code>字节，为了兼容性，一般不能超过<code>4095</code>字节。</p><p>IE 提供了一种存储可以持久化用户数据，叫做<code>uerData</code>，从<code>IE5.0</code>就开始支持。每个数据最多128K，每个域名下最多1M。这个持久化数据放在缓存中，如果缓存没有清理，那么会一直存在。</p><p>###优点：极高的扩展性和可用性</p><pre><code>1.通过良好的编程，控制保存在cookie中的session对象的大小。2.通过加密和安全传输技术（SSL），减少cookie被破解的可能性。3.只在cookie中存放不敏感数据，即使被盗也不会有重大损失。4.控制cookie的生命期，使之不会永远有效。偷盗者很可能拿到一个过期的cookie。</code></pre><p>###缺点：</p><pre><code>1.`Cookie`数量和长度的限制。每个domain最多只能有20条cookie，每个cookie长度不能超过4KB，否则会被截掉。2.安全性问题。如果cookie被人拦截了，那人就可以取得所有的session信息。即使加密也与事无补，因为拦截者并不需要知道cookie的意义，他只要原样转发cookie就可以达到目的了。3.有些状态不可能保存在客户端。例如，为了防止重复提交表单，我们需要在服务器端保存一个计数器。如果我们把这个计数器保存在客户端，那么它起不到任何作用。</code></pre><h2 id="2-浏览器本地存储"><a href="#2-浏览器本地存储" class="headerlink" title="2.浏览器本地存储"></a>2.浏览器本地存储</h2><p>在较高版本的浏览器中，<code>js</code>提供了<code>sessionStorage</code>和<code>globalStorage</code>。在<code>HTML5</code>中提供了<code>localStorage</code>来取代<code>globalStorage</code>。</p><p><code>html5</code>中的<code>Web Storage</code>包括了两种存储方式：<code>sessionStorage</code>和<code>localStorage</code>。</p><p><code>sessionStorage</code>用于本地存储一个会话（session）中的数据，这些数据只有在同一个会话中的页面才能访问并且当会话结束后数据也随之销毁。因此<code>sessionStorage</code>不是一种持久化的本地存储，仅仅是会话级别的存储。</p><p>而<code>localStorage</code>用于持久化的本地存储，除非主动删除数据，否则数据是永远不会过期的。</p><h2 id="3-web-storage和cookie的区别"><a href="#3-web-storage和cookie的区别" class="headerlink" title="3.web storage和cookie的区别"></a>3.web storage和cookie的区别</h2><p><code>Web Storage</code>的概念和<code>cookie</code>相似，区别是它是为了更大容量存储设计的。<code>Cookie</code>的大小是受限的，并且每次你请求一个新的页面的时候<code>Cookie</code>都会被发送过去，这样无形中浪费了带宽，另外<code>cookie</code>还需要指定作用域，不可以跨域调用。</p><p>除此之外，<code>Web Storage</code>拥有<code>setItem,getItem,removeItem,clear</code>等方法，不像<code>cookie</code>需要前端开发者自己封装<code>setCookie，getCookie</code>。</p><p>但是<code>Cookie</code>也是不可以或缺的：<code>Cookie</code>的作用是与服务器进行交互，作为<code>HTTP</code>规范的一部分而存在 ，而<code>Web Storage</code>仅仅是为了在本地“存储”数据而生</p><p>浏览器的支持除了<code>IE７</code>及以下不支持外，其他标准浏览器都完全支持(ie及FF需在web服务器里运行)，值得一提的是IE总是办好事，例如IE7、IE6中的<code>UserData</code>其实就是<code>javascript</code>本地存储的解决方案。通过简单的代码封装可以统一到所有的浏览器都支持<code>web storage</code>。</p><p><code>localStorage</code>和<code>sessionStorage</code>都具有相同的操作方法，例如<code>setItem、getItem</code>和<code>removeItem</code>等</p><h2 id="CSS-相关问题"><a href="#CSS-相关问题" class="headerlink" title="CSS 相关问题"></a>CSS 相关问题</h2><p>###display:none和visibility:hidden的区别？</p><pre><code>display:none  隐藏对应的元素，在文档布局中不再给它分配空间，它各边的元素会合拢，就当他从来不存在。visibility:hidden  隐藏对应的元素，但是在文档布局中仍保留原来的空间。</code></pre><p>###CSS中 link 和@import 的区别是？</p><pre><code>A：(1) link属于HTML标签，而@import是CSS提供的; (2) 页面被加载的时，link会同时被加载，而@import引用的CSS会等到页面被加载完再加载;(3) import只在IE5以上才能识别，而link是HTML标签，无兼容问题; (4) link方式的样式的权重 高于@import的权重.</code></pre><p>###position的absolute与fixed共同点与不同点    </p><pre><code>A：共同点：1.改变行内元素的呈现方式，display被置为block；2.让元素脱离普通流，不占据空间；3.默认会覆盖到非定位元素上B不同点：absolute的”根元素“是可以设置的，而fixed的”根元素“固定为浏览器窗口。当你滚动网页，fixed元素与浏览器窗口之间的距离是不变的。  </code></pre><p>###介绍一下CSS的盒子模型？</p><pre><code>1）有两种， IE 盒子模型、标准 W3C 盒子模型；IE的content部分包含了 border 和 pading;2）盒模型： 内容(content)、填充(padding)、边界(margin)、 边框(border).</code></pre><p>###CSS 选择符有哪些？哪些属性可以继承？优先级算法如何计算？ CSS3新增伪类有那些？</p><pre><code>   *   1.id选择器（ # myid）        2.类选择器（.myclassname）        3.标签选择器（div, h1, p）        4.相邻选择器（h1 + p）        5.子选择器（ul &gt; li）        6.后代选择器（li a）        7.通配符选择器（ * ）        8.属性选择器（a[rel = &quot;external&quot;]）        9.伪类选择器（a: hover, li:nth-child）        *   可继承的样式： font-size font-family color, text-indent;        *   不可继承的样式：border padding margin width height ;        *   优先级就近原则，同权重情况下样式定义最近者为准;        *   载入样式以最后载入的定位为准;优先级为:   !important &gt;  id &gt; class &gt; tag     important 比 内联优先级高,但内联比 id 要高CSS3新增伪类举例：p:first-of-type 选择属于其父元素的首个 &lt;p&gt; 元素的每个 &lt;p&gt; 元素。p:last-of-type  选择属于其父元素的最后 &lt;p&gt; 元素的每个 &lt;p&gt; 元素。p:only-of-type  选择属于其父元素唯一的 &lt;p&gt; 元素的每个 &lt;p&gt; 元素。p:only-child    选择属于其父元素的唯一子元素的每个 &lt;p&gt; 元素。p:nth-child(2)  选择属于其父元素的第二个子元素的每个 &lt;p&gt; 元素。:enabled  :disabled 控制表单控件的禁用状态。:checked        单选框或复选框被选中。</code></pre><p>###列出display的值，说明他们的作用。position的值， relative和absolute分别是相对于谁进行定位的？</p><pre><code>1.     block 象块类型元素一样显示。  inline 缺省值。象行内元素类型一样显示。  inline-block 象行内元素一样显示，但其内容象块类型元素一样显示。  list-item 象块类型元素一样显示，并添加样式列表标记。  2.   *absolute         生成绝对定位的元素，相对于 static 定位以外的第一个祖先元素进行定位。   *fixed （老IE不支持）        生成绝对定位的元素，相对于浏览器窗口进行定位。   *relative         生成相对定位的元素，相对于其在普通流中的位置进行定位。   * static  默认值。没有定位，元素出现在正常的流中  *（忽略 top, bottom, left, right z-index 声明）。  * inherit 规定从父元素继承 position 属性的值。</code></pre><p>###CSS3有哪些新特性？</p><pre><code>CSS3实现圆角（border-radius），阴影（box-shadow），对文字加特效（text-shadow、），线性渐变（gradient），旋转（transform）transform:rotate(9deg) scale(0.85,0.90) translate(0px,-30px) skew(-9deg,0deg);//旋转,缩放,定位,倾斜增加了更多的CSS选择器  多背景 rgba 在CSS3中唯一引入的伪元素是::selection.媒体查询，多栏布局border-image  </code></pre><p>###为什么要初始化CSS样式。</p><pre><code>    因为浏览器的兼容问题，不同浏览器对有些标签的默认值是不同的，如果没对CSS初始化往往会出现浏览器之间的页面显示差异。    当然，初始化样式会对SEO有一定的影响，但鱼和熊掌不可兼得，但力求影响最小的情况下初始化。*最简单的初始化方法就是： * &#123;padding: 0; margin: 0;&#125; （不建议）    淘宝的样式初始化：     body, h1, h2, h3, h4, h5, h6, hr, p, blockquote, dl, dt, dd, ul, ol, li, pre, form, fieldset, legend, button, input, textarea, th, td &#123; margin:0; padding:0; &#125;    body, button, input, select, textarea &#123; font:12px/1.5tahoma, arial, \5b8b\4f53; &#125;    h1, h2, h3, h4, h5, h6&#123; font-size:100%; &#125;    address, cite, dfn, em, var &#123; font-style:normal; &#125;    code, kbd, pre, samp &#123; font-family:couriernew, courier, monospace; &#125;    small&#123; font-size:12px; &#125;    ul, ol &#123; list-style:none; &#125;    a &#123; text-decoration:none; &#125;    a:hover &#123; text-decoration:underline; &#125;    sup &#123; vertical-align:text-top; &#125;    sub&#123; vertical-align:text-bottom; &#125;    legend &#123; color:#000; &#125;    fieldset, img &#123; border:0; &#125;    button, input, select, textarea &#123; font-size:100%; &#125;    table &#123; border-collapse:collapse; border-spacing:0; &#125; </code></pre><p>###对BFC规范的理解？<br>          BFC，块级格式化上下文，一个创建了新的BFC的盒子是独立布局的，盒子里面的子元素的样式不会影响到外面的元素。在同一个BFC中的两个毗邻的块级盒在垂直方向（和布局方向有关系）的margin会发生折叠。<br>        （W3C CSS 2.1 规范中的一个概念，它决定了元素如何对其内容进行布局，以及与其他元素的关系和相互作用。）</p><p>###解释下 CSS sprites，以及你要如何在页面或网站中使用它。</p><pre><code>CSS Sprites其实就是把网页中一些背景图片整合到一张图片文件中，再利用CSS的“background-image”，“background- repeat”，“background-position”的组合进行背景定位，background-position可以用数字能精确的定位出背景图片的位置。这样可以减少很多图片请求的开销，因为请求耗时比较长；请求虽然可以并发，但是也有限制，一般浏览器都是6个。对于未来而言，就不需要这样做了，因为有了`http2`。</code></pre><h2 id="html部分"><a href="#html部分" class="headerlink" title="html部分"></a>html部分</h2><p>###说说你对语义化的理解？</p><pre><code>1，去掉或者丢失样式的时候能够让页面呈现出清晰的结构2，有利于SEO：和搜索引擎建立良好沟通，有助于爬虫抓取更多的有效信息：爬虫依赖于标签来确定上下文和各个关键字的权重；3，方便其他设备解析（如屏幕阅读器、盲人阅读器、移动设备）以意义的方式来渲染网页；4，便于团队开发和维护，语义化更具可读性，是下一步吧网页的重要动向，遵循W3C标准的团队都遵循这个标准，可以减少差异化。</code></pre><p>###Doctype作用? 严格模式与混杂模式如何区分？它们有何意义?    </p><pre><code>（1）、&lt;!DOCTYPE&gt; 声明位于文档中的最前面，处于 &lt;html&gt; 标签之前。告知浏览器以何种模式来渲染文档。 （2）、严格模式的排版和 JS 运作模式是  以该浏览器支持的最高标准运行。（3）、在混杂模式中，页面以宽松的向后兼容的方式显示。模拟老式浏览器的行为以防止站点无法工作。（4）、DOCTYPE不存在或格式不正确会导致文档以混杂模式呈现。   </code></pre><p>###你知道多少种Doctype文档类型？</p><pre><code> 该标签可声明三种 DTD 类型，分别表示严格版本、过渡版本以及基于框架的 HTML 文档。 HTML 4.01 规定了三种文档类型：Strict、Transitional 以及 Frameset。 XHTML 1.0 规定了三种 XML 文档类型：Strict、Transitional 以及 Frameset。Standards （标准）模式（也就是严格呈现模式）用于呈现遵循最新标准的网页，而 Quirks （包容）模式（也就是松散呈现模式或者兼容模式）用于呈现为传统浏览器而设计的网页。</code></pre><h2 id="HTML与XHTML——二者有什么区别"><a href="#HTML与XHTML——二者有什么区别" class="headerlink" title="HTML与XHTML——二者有什么区别"></a>HTML与XHTML——二者有什么区别</h2><pre><code>区别：1.所有的标记都必须要有一个相应的结束标记2.所有标签的元素和属性的名字都必须使用小写3.所有的XML标记都必须合理嵌套4.所有的属性必须用引号&quot;&quot;括起来5.把所有&lt;和&amp;特殊符号用编码表示6.给所有属性赋一个值7.不要在注释内容中使“--”8.图片必须有说明文字</code></pre><h2 id="常见兼容性问题？"><a href="#常见兼容性问题？" class="headerlink" title="常见兼容性问题？"></a>常见兼容性问题？</h2><pre><code>* png24位的图片在iE6浏览器上出现背景，解决方案是做成PNG8.也可以引用一段脚本处理.* 浏览器默认的margin和padding不同。解决方案是加一个全局的*&#123;margin:0;padding:0;&#125;来统一。* IE6双边距bug:块属性标签float后，又有横行的margin情况下，在ie6显示margin比设置的大。 * 浮动ie产生的双倍距离（IE6双边距问题：在IE6下，如果对元素设置了浮动，同时又设置了margin-left或margin-right，margin值会加倍。）  #box&#123; float:left; width:10px; margin:0 0 0 100px;&#125;  这种情况之下IE会产生20px的距离，解决方案是在float的标签样式控制中加入 ——_display:inline;将其转化为行内属性。(_这个符号只有ie6会识别)*  渐进识别的方式，从总体中逐渐排除局部。   首先，巧妙的使用“\9”这一标记，将IE游览器从所有情况中分离出来。   接着，再次使用“+”将IE8和IE7、IE6分离开来，这样IE8已经独立识别。  css      .bb&#123;       background-color:#f1ee18;/*所有识别*/      .background-color:#00deff\9; /*IE6、7、8识别*/      +background-color:#a200ff;/*IE6、7识别*/      _background-color:#1e0bd1;/*IE6识别*/       &#125; *  IE下,可以使用获取常规属性的方法来获取自定义属性,   也可以使用getAttribute()获取自定义属性;   Firefox下,只能使用getAttribute()获取自定义属性.    解决方法:统一通过getAttribute()获取自定义属性.* IE下,event对象有x,y属性,但是没有pageX,pageY属性;   Firefox下,event对象有pageX,pageY属性,但是没有x,y属性.* 解决方法：（条件注释）缺点是在IE浏览器下可能会增加额外的HTTP请求数。* Chrome 中文界面下默认会将小于 12px 的文本强制按照 12px 显示,   可通过加入 CSS 属性 -webkit-text-size-adjust: none; 解决.* 超链接访问过后hover样式就不出现了 被点击访问过的超链接样式不在具有hover和active了解决方法是改变CSS属性的排列顺序:L-V-H-A :  a:link &#123;&#125; a:visited &#123;&#125; a:hover &#123;&#125; a:active &#123;&#125;* 怪异模式问题：漏写DTD声明，Firefox仍然会按照标准模式来解析网页，但在IE中会触发怪异模式。为避免怪异模式给我们带来不必要的麻烦，最好养成书写DTD声明的好习惯。现在可以使用[html5](http://www.w3.org/TR/html5/single-page.html)推荐的写法：`&lt;doctype html&gt;`* 上下margin重合问题ie和ff都存在，相邻的两个div的margin-left和margin-right不会重合，但是margin-top和margin-bottom却会发生重合。解决方法，养成良好的代码编写习惯，同时采用margin-top或者同时采用margin-bottom。* ie6对png图片格式支持不好(引用一段脚本处理)</code></pre><p>###解释下浮动和它的工作原理？清除浮动的技巧</p><pre><code>浮动元素脱离文档流，不占据空间。浮动元素碰到包含它的边框或者浮动元素的边框停留。1.使用空标签清除浮动。   这种方法是在所有浮动标签后面添加一个空标签 定义css clear:both. 弊端就是增加了无意义标签。2.使用overflow。   给包含浮动元素的父标签添加css属性 overflow:auto; zoom:1; zoom:1用于兼容IE6。3.使用after伪对象清除浮动。   该方法只适用于非IE浏览器。具体写法可参照以下示例。使用中需注意以下几点。一、该方法中必须为需要清除浮动元素的伪对象中设置 height:0，否则该元素会比实际高出若干像素；</code></pre><p>###浮动元素引起的问题和解决办法？</p><pre><code>浮动元素引起的问题：（1）父元素的高度无法被撑开，影响与父元素同级的元素（2）与浮动元素同级的非浮动元素会跟随其后（3）若非第一个元素浮动，则该元素之前的元素也需要浮动，否则会影响页面显示的结构</code></pre><p>解决方法：<br>使用<code>CSS</code>中的<code>clear:both</code>;属性来清除元素的浮动可解决2、3问题，对于问题1，添加如下样式，给父元素添加<code>clearfix</code>样式：</p><pre><code>.clearfix:after&#123;content: &quot;.&quot;;display: block;height: 0;clear: both;visibility: hidden;&#125;.clearfix&#123;display: inline-block;&#125; /* for IE/Mac */</code></pre><p><strong>清除浮动的几种方法：</strong></p><pre><code>1，额外标签法，&lt;div style=&quot;clear:both;&quot;&gt;&lt;/div&gt;（缺点：不过这个办法会增加额外的标签使HTML结构看起来不够简洁。）2，使用after伪类#parent:after&#123;    content:&quot;.&quot;;    height:0;    visibility:hidden;    display:block;    clear:both;    &#125;3,浮动外部元素4,设置`overflow`为`hidden`或者auto</code></pre><p>###IE 8以下版本的浏览器中的盒模型有什么不同</p><pre><code>IE8以下浏览器的盒模型中定义的元素的宽高不包括内边距和边框</code></pre><p>###DOM操作——怎样添加、移除、移动、复制、创建和查找节点。 </p><pre><code>（1）创建新节点      createDocumentFragment()    //创建一个DOM片段      createElement()   //创建一个具体的元素      createTextNode()   //创建一个文本节点（2）添加、移除、替换、插入      appendChild()      removeChild()      replaceChild()      insertBefore() //在已有的子节点前插入一个新的子节点（3）查找      getElementsByTagName()    //通过标签名称      getElementsByName()    //通过元素的Name属性的值(IE容错能力较强，会得到一个数组，其中包括id等于name值的)      getElementById()    //通过元素Id，唯一性</code></pre><p>###html5有哪些新特性、移除了那些元素？如何处理HTML5新标签的浏览器兼容问题？如何区分 HTML 和 HTML5？</p><pre><code>* HTML5 现在已经不是 SGML 的子集，主要是关于图像，位置，存储，多任务等功能的增加。* 拖拽释放(Drag and drop) API   语义化更好的内容标签（header,nav,footer,aside,article,section）  音频、视频API(audio,video)  画布(Canvas) API  地理(Geolocation) API  本地离线存储 localStorage 长期存储数据，浏览器关闭后数据不丢失；  sessionStorage 的数据在浏览器关闭后自动删除  表单控件，calendar、date、time、email、url、search    新的技术webworker, websocket, Geolocation* 移除的元素纯表现的元素：basefont，big，center，font, s，strike，tt，u；对可用性产生负面影响的元素：frame，frameset，noframes；支持HTML5新标签：* IE8/IE7/IE6支持通过document.createElement方法产生的标签，  可以利用这一特性让这些浏览器支持HTML5新标签，  浏览器支持新标签后，还需要添加标签默认的样式：* 当然最好的方式是直接使用成熟的框架、使用最多的是html5shim框架   &lt;!--[if lt IE 9]&gt;    &lt;script&gt; src=&quot;http://html5shim.googlecode.com/svn/trunk/html5.js&quot;&lt;/script&gt;    &lt;![endif]--&gt; 如何区分： DOCTYPE声明\新增的结构元素\功能元素</code></pre><h2 id="iframe的优缺点？"><a href="#iframe的优缺点？" class="headerlink" title="iframe的优缺点？"></a>iframe的优缺点？</h2><pre><code>1.&lt;iframe&gt;优点：    解决加载缓慢的第三方内容如图标和广告等的加载问题    Security sandbox    并行加载脚本2.&lt;iframe&gt;的缺点：     *iframe会阻塞主页面的Onload事件；        *即时内容为空，加载也需要时间    *没有语意 </code></pre><h2 id="如何实现浏览器内多个标签页之间的通信"><a href="#如何实现浏览器内多个标签页之间的通信" class="headerlink" title="如何实现浏览器内多个标签页之间的通信?"></a>如何实现浏览器内多个标签页之间的通信?</h2><pre><code>调用localstorge、cookies等本地存储方式</code></pre><h2 id="webSocket如何兼容低浏览器？"><a href="#webSocket如何兼容低浏览器？" class="headerlink" title="webSocket如何兼容低浏览器？"></a>webSocket如何兼容低浏览器？</h2><pre><code>Adobe Flash Socket 、 ActiveX HTMLFile (IE) 、 基于 multipart 编码发送 XHR 、 基于长轮询的 XHR</code></pre><h2 id="线程与进程的区别"><a href="#线程与进程的区别" class="headerlink" title="线程与进程的区别"></a>线程与进程的区别</h2><pre><code>一个程序至少有一个进程,一个进程至少有一个线程. 线程的划分尺度小于进程，使得多线程程序的并发性高。 另外，进程在执行过程中拥有独立的内存单元，而多个线程共享内存，从而极大地提高了程序的运行效率。 线程在执行过程中与进程还是有区别的。每个独立的线程有一个程序运行的入口、顺序执行序列和程序的出口。但是线程不能够独立执行，必须依存在应用程序中，由应用程序提供多个线程执行控制。 从逻辑角度来看，多线程的意义在于一个应用程序中，有多个执行部分可以同时执行。但操作系统并没有将多个线程看做多个独立的应用，来实现进程的调度和管理以及资源分配。这就是进程和线程的重要区别。</code></pre><h2 id="你如何对网站的文件和资源进行优化？"><a href="#你如何对网站的文件和资源进行优化？" class="headerlink" title="你如何对网站的文件和资源进行优化？"></a>你如何对网站的文件和资源进行优化？</h2><pre><code>期待的解决方案包括： 文件合并 文件最小化/文件压缩 使用 CDN 托管 缓存的使用（多个域名来提供缓存） 其他</code></pre><h2 id="请说出三种减少页面加载时间的方法。"><a href="#请说出三种减少页面加载时间的方法。" class="headerlink" title="请说出三种减少页面加载时间的方法。"></a>请说出三种减少页面加载时间的方法。</h2><pre><code> 1.优化图片  2.图像格式的选择（GIF：提供的颜色较少，可用在一些对颜色要求不高的地方）  3.优化CSS（压缩合并css，如margin-top,margin-left...)  4.网址后加斜杠（如www.campr.com/目录，会判断这个“目录是什么文件类型，或者是目录。）  5.标明高度和宽度（如果浏览器没有找到这两个参数，它需要一边下载图片一边计算大小，如果图片很多，浏览器需要不断地调整页面。这不但影响速度，也影响浏览体验。 当浏览器知道了高度和宽度参数后，即使图片暂时无法显示，页面上也会腾出图片的空位，然后继续加载后面的内容。从而加载时间快了，浏览体验也更好了。） 6.减少http请求（合并文件，合并图片）。</code></pre><h2 id="你都使用哪些工具来测试代码的性能？"><a href="#你都使用哪些工具来测试代码的性能？" class="headerlink" title="你都使用哪些工具来测试代码的性能？"></a>你都使用哪些工具来测试代码的性能？</h2><pre><code>Profiler, JSPerf（http://jsperf.com/nexttick-vs-setzerotimeout-vs-settimeout）, Dromaeo</code></pre><h2 id="什么是-FOUC（无样式内容闪烁）？你如何来避免-FOUC？"><a href="#什么是-FOUC（无样式内容闪烁）？你如何来避免-FOUC？" class="headerlink" title="什么是 FOUC（无样式内容闪烁）？你如何来避免 FOUC？"></a>什么是 FOUC（无样式内容闪烁）？你如何来避免 FOUC？</h2><pre><code> FOUC - Flash Of Unstyled Content 文档样式闪烁 &lt;style type=&quot;text/css&quot; media=&quot;all&quot;&gt;@import &quot;../fouc.css&quot;;&lt;/style&gt; 而引用CSS文件的@import就是造成这个问题的罪魁祸首。IE会先加载整个HTML文档的DOM，然后再去导入外部的CSS文件，因此，在页面DOM加载完成到CSS导入完成中间会有一段时间页面上的内容是没有样式的，这段时间的长短跟网速，电脑速度都有关系。 解决方法简单的出奇，只要在&lt;head&gt;之间加入一个&lt;link&gt;或者&lt;script&gt;元素就可以了。</code></pre><h2 id="null和undefined的区别？"><a href="#null和undefined的区别？" class="headerlink" title="null和undefined的区别？"></a>null和undefined的区别？</h2><p><code>null</code>是一个表示”无”的对象，转为数值时为0；<code>undefined</code>是一个表示”无”的原始值，转为数值时为<code>NaN</code>。  </p><p>当声明的变量还未被初始化时，变量的默认值为<code>undefined</code>。<br><code>null</code>用来表示尚未存在的对象，常用来表示函数企图返回一个不存在的对象。 </p><p><code>undefined</code>表示”缺少值”，就是此处应该有一个值，但是还没有定义。典型用法是：</p><pre><code>（1）变量被声明了，但没有赋值时，就等于undefined。（2) 调用函数时，应该提供的参数没有提供，该参数等于undefined。（3）对象没有赋值的属性，该属性的值为undefined。（4）函数没有返回值时，默认返回undefined。</code></pre><p><code>null</code>表示”没有对象”，即该处不应该有值。典型用法是：</p><pre><code>（1） 作为函数的参数，表示该函数的参数不是对象。（2） 作为对象原型链的终点。</code></pre><h2 id="new操作符具体干了什么呢"><a href="#new操作符具体干了什么呢" class="headerlink" title="new操作符具体干了什么呢?"></a>new操作符具体干了什么呢?</h2><pre><code>   1、创建一个空对象，并且 this 变量引用该对象，同时还继承了该函数的原型。   2、属性和方法被加入到 this 引用的对象中。   3、新创建的对象由 this 所引用，并且最后隐式的返回 this 。var obj  = &#123;&#125;;obj.__proto__ = Base.prototype;Base.call(obj); </code></pre><h2 id="JSON-的了解？"><a href="#JSON-的了解？" class="headerlink" title="JSON 的了解？"></a>JSON 的了解？</h2><pre><code>JSON(JavaScript Object Notation) 是一种轻量级的数据交换格式。它是基于JavaScript的一个子集。数据格式简单, 易于读写, 占用带宽小&#123;&#39;age&#39;:&#39;12&#39;, &#39;name&#39;:&#39;back&#39;&#125;</code></pre><h2 id="js延迟加载的方式有哪些？"><a href="#js延迟加载的方式有哪些？" class="headerlink" title="js延迟加载的方式有哪些？"></a>js延迟加载的方式有哪些？</h2><pre><code>defer和async、动态创建DOM方式（创建script，插入到DOM中，加载完毕后callBack）、按需异步载入js</code></pre><h2 id="如何解决跨域问题"><a href="#如何解决跨域问题" class="headerlink" title="如何解决跨域问题?"></a>如何解决跨域问题?</h2><pre><code>    jsonp、 document.domain+iframe、window.name、window.postMessage、服务器上设置代理页面    jsonp的原理是动态插入script标签</code></pre><p>具体参见：<a href="http://segmentfault.com/blog/trigkit4/1190000000718840">详解js跨域问题</a></p><h2 id="documen-write和-innerHTML的区别"><a href="#documen-write和-innerHTML的区别" class="headerlink" title="documen.write和 innerHTML的区别"></a>documen.write和 innerHTML的区别</h2><pre><code>document.write只能重绘整个页面innerHTML可以重绘页面的一部分</code></pre><h2 id="call-和-apply-的区别和作用？"><a href="#call-和-apply-的区别和作用？" class="headerlink" title=".call() 和 .apply() 的区别和作用？"></a>.call() 和 .apply() 的区别和作用？</h2><p>作用：动态改变某个类的某个方法的运行环境。<br>区别参见：<a href="http://segmentfault.com/blog/trigkit4/1190000000660786#articleHeader15">JavaScript学习总结（四）function函数部分</a></p><h2 id="哪些操作会造成内存泄漏？"><a href="#哪些操作会造成内存泄漏？" class="headerlink" title="哪些操作会造成内存泄漏？"></a>哪些操作会造成内存泄漏？</h2><pre><code>内存泄漏指任何对象在您不再拥有或需要它之后仍然存在。垃圾回收器定期扫描对象，并计算引用了每个对象的其他对象的数量。如果一个对象的引用数量为 0（没有其他对象引用过该对象），或对该对象的惟一引用是循环的，那么该对象的内存即可回收。setTimeout 的第一个参数使用字符串而非函数的话，会引发内存泄漏。闭包、控制台日志、循环（在两个对象彼此引用且彼此保留时，就会产生一个循环）</code></pre><p>详见：<a href="http://segmentfault.com/blog/trigkit4/1190000000687844">详解js变量、作用域及内存</a></p><h2 id="JavaScript中的作用域与变量声明提升？"><a href="#JavaScript中的作用域与变量声明提升？" class="headerlink" title="JavaScript中的作用域与变量声明提升？"></a>JavaScript中的作用域与变量声明提升？</h2><p>详见：<a href="http://segmentfault.com/blog/trigkit4/1190000000758184#articleHeader5">详解JavaScript函数模式</a></p><h2 id="如何判断当前脚本运行在浏览器还是node环境中？"><a href="#如何判断当前脚本运行在浏览器还是node环境中？" class="headerlink" title="如何判断当前脚本运行在浏览器还是node环境中？"></a>如何判断当前脚本运行在浏览器还是node环境中？</h2><pre><code>通过判断Global对象是否为window，如果不为window，当前脚本没有运行在浏览器中</code></pre><h2 id="其他问题？"><a href="#其他问题？" class="headerlink" title="其他问题？"></a>其他问题？</h2><p>###你遇到过比较难的技术问题是？你是如何解决的？</p><p>###常使用的库有哪些？常用的前端开发工具？开发过什么应用或组件？</p><p>###列举IE 与其他浏览器不一样的特性？</p><p>###99%的网站都需要被重构是那本书上写的？</p><pre><code>* 网站重构：应用web标准进行设计（第2版）</code></pre><p>###什么叫优雅降级和渐进增强？</p><pre><code>优雅降级：Web站点在所有新式浏览器中都能正常工作，如果用户使用的是老式浏览器，则代码会检查以确认它们是否能正常工作。由于IE独特的盒模型布局问题，针对不同版本的IE的hack实践过优雅降级了,为那些无法支持功能的浏览器增加候选方案，使之在旧式浏览器上以某种形式降级体验却不至于完全失效.渐进增强：从被所有浏览器支持的基本功能开始，逐步地添加那些只有新式浏览器才支持的功能,向页面增加无害于基础浏览器的额外样式和功能的。当浏览器支持时，它们会自动地呈现出来并发挥作用。</code></pre><p>详见：<a href="http://segmentfault.com/blog/trigkit4/1190000000800711#articleHeader30">css学习归纳总结（一）</a></p><p>###WEB应用从服务器主动推送Data到客户端有那些方式？</p><p>###对Node的优点和缺点提出了自己的看法？</p><pre><code>*（优点）因为Node是基于事件驱动和无阻塞的，所以非常适合处理并发请求，  因此构建在Node上的代理服务器相比其他技术实现（如Ruby）的服务器表现要好得多。  此外，与Node代理服务器交互的客户端代码是由javascript语言编写的，  因此客户端和服务器端都用同一种语言编写，这是非常美妙的事情。*（缺点）Node是一个相对新的开源项目，所以不太稳定，它总是一直在变，  而且缺少足够多的第三方库支持。看起来，就像是Ruby/Rails当年的样子。</code></pre><p>###除了前端以外还了解什么其它技术么？你最最厉害的技能是什么？</p><p>###你常用的开发工具是什么，为什么？</p><p>###对前端界面工程师这个职位是怎么样理解的？它的前景会怎么样？</p><pre><code>前端是最贴近用户的程序员，比后端、数据库、产品经理、运营、安全都近。    1、实现界面交互    2、提升用户体验    3、有了Node.js，前端可以实现服务端的一些事情前端是最贴近用户的程序员，前端的能力就是能让产品从 90分进化到 100 分，甚至更好， 参与项目，快速高质量完成实现效果图，精确到1px； 与团队成员，UI设计，产品经理的沟通； 做好的页面结构，页面重构和用户体验； 处理hack，兼容、写出优美的代码格式； 针对服务器的优化、拥抱最新前端技术。</code></pre><p>###你在现在的团队处于什么样的角色，起到了什么明显的作用？</p><p>###你认为怎样才是全端工程师（Full Stack developer）？</p><p>###介绍一个你最得意的作品吧？<br>###项目中遇到什么问题？如何解决？<br>###你的优点是什么？缺点是什么？</p><p>###如何管理前端团队?</p><p>###最近在学什么？能谈谈你未来3，5年给自己的规划吗？</p><h2 id="你有哪些性能优化的方法？"><a href="#你有哪些性能优化的方法？" class="headerlink" title="你有哪些性能优化的方法？"></a>你有哪些性能优化的方法？</h2><p> （<a href="http://segmentfault.com/blog/trigkit4/1190000000656717">详情请看雅虎14条性能优化原则</a>）。</p><pre><code>  （1） 减少http请求次数：CSS Sprites, JS、CSS源码压缩、图片大小控制合适；网页Gzip，CDN托管，data缓存 ，图片服务器。  （2） 前端模板 JS+数据，减少由于HTML标签导致的带宽浪费，前端用变量保存AJAX请求结果，每次操作本地变量，不用请求，减少请求次数  （3） 用innerHTML代替DOM操作，减少DOM操作次数，优化javascript性能。  （4） 当需要设置的样式很多时设置className而不是直接操作style。  （5） 少用全局变量、缓存DOM节点查找的结果。减少IO读取操作。  （6） 避免使用CSS Expression（css表达式)又称Dynamic properties(动态属性)。  （7） 图片预加载，将样式表放在顶部，将脚本放在底部  加上时间戳。</code></pre><h2 id="http状态码有那些？分别代表是什么意思？"><a href="#http状态码有那些？分别代表是什么意思？" class="headerlink" title="http状态码有那些？分别代表是什么意思？"></a>http状态码有那些？分别代表是什么意思？</h2><pre><code>100-199 用于指定客户端应相应的某些动作。 200-299 用于表示请求成功。 300-399 用于已经移动的文件并且常被包含在定位头信息中指定新的地址信息。 400-499 用于指出客户端的错误。400    1、语义有误，当前请求无法被服务器理解。401   当前请求需要用户验证 403  服务器已经理解请求，但是拒绝执行它。500-599 用于支持服务器错误。 503 – 服务不可用</code></pre><p>详情：<a href="http://segmentfault.com/blog/trigkit4/1190000000691919">http://segmentfault.com/blog/trigkit4/1190000000691919</a></p><h2 id="一个页面从输入-URL-到页面加载显示完成，这个过程中都发生了什么？"><a href="#一个页面从输入-URL-到页面加载显示完成，这个过程中都发生了什么？" class="headerlink" title="一个页面从输入 URL 到页面加载显示完成，这个过程中都发生了什么？"></a>一个页面从输入 URL 到页面加载显示完成，这个过程中都发生了什么？</h2><pre><code>    分为4个步骤：    （1），当发送一个URL请求时，不管这个URL是Web页面的URL还是Web页面上每个资源的URL，浏览器都会开启一个线程来处理这个请求，同时在远程DNS服务器上启动一个DNS查询。这能使浏览器获得请求对应的IP地址。    （2）， 浏览器与远程Web服务器通过TCP三次握手协商来建立一个TCP/IP连接。该握手包括一个同步报文，一个同步-应答报文和一个应答报文，这三个报文在 浏览器和服务器之间传递。该握手首先由客户端尝试建立起通信，而后服务器应答并接受客户端的请求，最后由客户端发出该请求已经被接受的报文。    （3），一旦TCP/IP连接建立，浏览器会通过该连接向远程服务器发送HTTP的GET请求。远程服务器找到资源并使用HTTP响应返回该资源，值为200的HTTP响应状态表示一个正确的响应。    （4），此时，Web服务器提供资源服务，客户端开始下载资源。    请求返回后，便进入了我们关注的前端模块简单来说，浏览器会解析HTML生成DOM Tree，其次会根据CSS生成CSS Rule Tree，而javascript又可以根据DOM API操作DOM</code></pre><p>详情：<a href="https://juejin.im/post/5c7646f26fb9a049fd108380">https://juejin.im/post/5c7646f26fb9a049fd108380</a></p><h2 id="平时如何管理你的项目？"><a href="#平时如何管理你的项目？" class="headerlink" title="平时如何管理你的项目？"></a>平时如何管理你的项目？</h2><pre><code>先期团队必须确定好全局样式（globe.css），编码模式(utf-8) 等；        编写习惯必须一致（例如都是采用继承式的写法，单样式都写成一行）；        标注样式编写人，各模块都及时标注（标注关键样式调用的地方）；        页面进行标注（例如 页面 模块 开始和结束）；        CSS跟HTML 分文件夹并行存放，命名都得统一（例如style.css）；        JS 分文件夹存放 命名以该JS功能为准的英文翻译。        图片采用整合的 images.png png8 格式文件使用 尽量整合在一起使用方便将来的管理         </code></pre><h2 id="说说最近最流行的一些东西吧？常去哪些网站？"><a href="#说说最近最流行的一些东西吧？常去哪些网站？" class="headerlink" title="说说最近最流行的一些东西吧？常去哪些网站？"></a>说说最近最流行的一些东西吧？常去哪些网站？</h2><pre><code>Node.js、Mongodb、npm、MVVM、MEAN、three.js,React 。网站：w3cfuns,sf,hacknews,CSDN,慕课，博客园，InfoQ,w3cplus等</code></pre><h2 id="javascript对象的几种创建方式"><a href="#javascript对象的几种创建方式" class="headerlink" title="javascript对象的几种创建方式"></a>javascript对象的几种创建方式</h2><pre><code>1，工厂模式2，构造函数模式3，原型模式4，混合构造函数和原型模式5，动态原型模式6，寄生构造函数模式7，稳妥构造函数模式</code></pre><h2 id="javascript继承的6种方法"><a href="#javascript继承的6种方法" class="headerlink" title="javascript继承的6种方法"></a>javascript继承的6种方法</h2><pre><code>1，原型链继承2，借用构造函数继承3，组合继承(原型+借用构造)4，原型式继承5，寄生式继承6，寄生组合式继承</code></pre><p>详情：<a href="http://segmentfault.com/blog/trigkit4/1190000002440502">JavaScript继承方式详解</a><br>ajax过程</p><hr><pre><code>(1)创建XMLHttpRequest对象,也就是创建一个异步调用对象.(2)创建一个新的HTTP请求,并指定该HTTP请求的方法、URL及验证信息.(3)设置响应HTTP请求状态变化的函数.(4)发送HTTP请求.(5)获取异步调用返回的数据.(6)使用JavaScript和DOM实现局部刷新.</code></pre><p>详情：<a href="http://segmentfault.com/blog/trigkit4/1190000000691919">JavaScript学习总结（七）Ajax和Http状态字</a></p><h2 id="异步加载和延迟加载"><a href="#异步加载和延迟加载" class="headerlink" title="异步加载和延迟加载"></a>异步加载和延迟加载</h2><pre><code>1.异步加载的方案： 动态插入script标签2.通过ajax去获取js代码，然后通过eval执行3.script标签上添加defer或者async属性4.创建并插入iframe，让它异步执行js5.延迟加载：有些 js 代码并不是页面初始化的时候就立刻需要的，而稍后的某些情况才需要的。</code></pre><h2 id="前端安全问题？"><a href="#前端安全问题？" class="headerlink" title="前端安全问题？"></a>前端安全问题？</h2><pre><code>    （XSS，sql注入，CSRF）CSRF：是跨站请求伪造，很明显根据刚刚的解释，他的核心也就是请求伪造，通过伪造身份提交POST和GET请求来进行跨域的攻击。**完成CSRF需要两个步骤：**1.登陆受信任的网站A，在本地生成COOKIE2.在不登出A的情况下，或者本地COOKIE没有过期的情况下，访问危险网站B。</code></pre><h2 id="ie各版本和chrome可以并行下载多少个资源"><a href="#ie各版本和chrome可以并行下载多少个资源" class="headerlink" title="ie各版本和chrome可以并行下载多少个资源"></a>ie各版本和chrome可以并行下载多少个资源</h2><pre><code>IE6 两个并发，iE7升级之后的6个并发，之后版本也是6个Firefox，chrome也是6个</code></pre><h2 id="javascript里面的继承怎么实现，如何避免原型链上面的对象共享"><a href="#javascript里面的继承怎么实现，如何避免原型链上面的对象共享" class="headerlink" title="javascript里面的继承怎么实现，如何避免原型链上面的对象共享"></a>javascript里面的继承怎么实现，如何避免原型链上面的对象共享</h2><pre><code>用构造函数和原型链的混合模式去实现继承，避免对象共享可以参考经典的extend()函数，很多前端框架都有封装的，就是用一个空函数当做中间变量</code></pre><h2 id="grunt，-YUI-compressor-和-google-clojure用来进行代码压缩的用法。"><a href="#grunt，-YUI-compressor-和-google-clojure用来进行代码压缩的用法。" class="headerlink" title="grunt， YUI compressor 和 google clojure用来进行代码压缩的用法。"></a>grunt， YUI compressor 和 google clojure用来进行代码压缩的用法。</h2><pre><code>YUI Compressor 是一个用来压缩 JS 和 CSS 文件的工具，采用Java开发。使用方法：//压缩JSjava -jar yuicompressor-2.4.2.jar --type js --charset utf-8 -v src.js &gt; packed.js//压缩CSSjava -jar yuicompressor-2.4.2.jar --type css --charset utf-8 -v src.css &gt; packed.css</code></pre><p>详情请见：<a href="http://segmentfault.com/blog/trigkit4/1190000002585760">你需要掌握的前端代码性能优化工具</a> </p><h2 id="Flash、Ajax各自的优缺点，在使用中如何取舍？"><a href="#Flash、Ajax各自的优缺点，在使用中如何取舍？" class="headerlink" title="Flash、Ajax各自的优缺点，在使用中如何取舍？"></a>Flash、Ajax各自的优缺点，在使用中如何取舍？</h2><pre><code>1、Flash ajax对比Flash适合处理多媒体、矢量图形、访问机器；对CSS、处理文本上不足，不容易被搜索。Ajax对CSS、文本支持很好，支持搜索；多媒体、矢量图形、机器访问不足。共同点：与服务器的无刷新传递消息、用户离线和在线状态、操作DOM</code></pre><h2 id="请解释一下-JavaScript-的同源策略。"><a href="#请解释一下-JavaScript-的同源策略。" class="headerlink" title="请解释一下 JavaScript 的同源策略。"></a>请解释一下 JavaScript 的同源策略。</h2><p>概念:同源策略是客户端脚本（尤其是<code>Javascript</code>）的重要的安全度量标准。它最早出自<code>Netscape Navigator2.0</code>，其目的是防止某个文档或脚本从多个不同源装载。</p><p>这里的同源策略指的是：协议，域名，端口相同，同源策略是一种安全协议。<br>指一段脚本只能读取来自同一来源的窗口和文档的属性。</p><p>###为什么要有同源限制？<br>   我们举例说明：比如一个黑客程序，他利用<code>Iframe</code>把真正的银行登录页面嵌到他的页面上，当你使用真实的用户名，密码登录时，他的页面就可以通过<code>Javascript</code>读取到你的表单中<code>input</code>中的内容，这样用户名，密码就轻松到手了。</p><h2 id="什么是-“use-strict”-使用它的好处和坏处分别是什么？"><a href="#什么是-“use-strict”-使用它的好处和坏处分别是什么？" class="headerlink" title="什么是 “use strict”; ? 使用它的好处和坏处分别是什么？"></a>什么是 “use strict”; ? 使用它的好处和坏处分别是什么？</h2><p><code>ECMAscript 5</code>添加了第二种运行模式：”严格模式”（strict mode）。顾名思义，这种模式使得<code>Javascript</code>在更严格的条件下运行。</p><p>设立”严格模式”的目的，主要有以下几个：</p><pre><code>- 消除Javascript语法的一些不合理、不严谨之处，减少一些怪异行为;- 消除代码运行的一些不安全之处，保证代码运行的安全；- 提高编译器效率，增加运行速度；- 为未来新版本的Javascript做好铺垫。</code></pre><p>注：经过测试<code>IE6,7,8,9</code>均不支持严格模式。</p><p>缺点：<br>现在网站的<code>JS</code> 都会进行压缩，一些文件用了严格模式，而另一些没有。这时这些本来是严格模式的文件，被 <code>merge</code> 后，这个串就到了文件的中间，不仅没有指示严格模式，反而在压缩后浪费了字节。</p><h2 id="GET和POST的区别，何时使用POST？"><a href="#GET和POST的区别，何时使用POST？" class="headerlink" title="GET和POST的区别，何时使用POST？"></a>GET和POST的区别，何时使用POST？</h2><pre><code>    GET：一般用于信息获取，使用URL传递参数，对所发送信息的数量也有限制，一般在2000个字符    POST：一般用于修改服务器上的资源，对所发送的信息没有限制。        GET方式需要使用Request.QueryString来取得变量的值，而POST方式通过Request.Form来获取变量的值，    也就是说Get是通过地址栏来传值，而Post是通过提交表单来传值。然而，在以下情况中，请使用 POST 请求：无法使用缓存文件（更新服务器上的文件或数据库）向服务器发送大量数据（POST 没有数据量限制）发送包含未知字符的用户输入时，POST 比 GET 更稳定也更可靠</code></pre><h2 id="哪些地方会出现css阻塞，哪些地方会出现js阻塞？"><a href="#哪些地方会出现css阻塞，哪些地方会出现js阻塞？" class="headerlink" title="哪些地方会出现css阻塞，哪些地方会出现js阻塞？"></a>哪些地方会出现css阻塞，哪些地方会出现js阻塞？</h2><p><strong>js的阻塞特性：</strong>所有浏览器在下载<code>JS</code>的时候，会阻止一切其他活动，比如其他资源的下载，内容的呈现等等。直到<code>JS</code>下载、解析、执行完毕后才开始继续<code>并行下载</code>其他资源并呈现内容。为了提高用户体验，新一代浏览器都支持并行下载<code>JS</code>，但是<code>JS</code>下载仍然会阻塞其它资源的下载（例如.图片，css文件等）。</p><p>由于浏览器为了防止出现<code>JS</code>修改<code>DOM</code>树，需要重新构建<code>DOM</code>树的情况，所以就会阻塞其他的下载和呈现。</p><p>嵌入<code>JS</code>会阻塞所有内容的呈现，而外部<code>JS</code>只会阻塞其后内容的显示，2种方式都会阻塞其后资源的下载。也就是说外部样式不会阻塞外部脚本的加载，但会阻塞外部脚本的执行。</p><p><code>CSS</code>怎么会阻塞加载了？<code>CSS</code>本来是可以并行下载的，在什么情况下会出现阻塞加载了(在测试观察中，<code>IE6</code>下<code>CSS</code>都是阻塞加载）</p><p>当<code>CSS</code>后面跟着嵌入的<code>JS</code>的时候，该<code>CSS</code>就会出现阻塞后面资源下载的情况。而当把嵌入<code>JS</code>放到<code>CSS</code>前面，就不会出现阻塞的情况了。</p><p> 根本原因：因为浏览器会维持<code>html</code>中<code>css</code>和<code>js</code>的顺序，样式表必须在嵌入的JS执行前先加载、解析完。而嵌入的<code>JS</code>会阻塞后面的资源加载，所以就会出现上面<code>CSS</code>阻塞下载的情况。</p><p>嵌入<code>JS</code>应该放在什么位置？</p><pre><code>   1、放在底部，虽然放在底部照样会阻塞所有呈现，但不会阻塞资源下载。      2、如果嵌入JS放在head中，请把嵌入JS放在CSS头部。      3、使用defer（只支持IE）      4、不要在嵌入的JS中调用运行时间较长的函数，如果一定要用，可以用`setTimeout`来调用</code></pre><p>###Javascript无阻塞加载具体方式</p><ul><li><p><strong>将脚本放在底部。</strong><code>&lt;link&gt;</code>还是放在<code>head</code>中，用以保证在<code>js</code>加载前，能加载出正常显示的页面。<code>&lt;script&gt;</code>标签放在<code>&lt;/body&gt;</code>前。</p></li><li><p><strong>成组脚本</strong>：由于每个<code>&lt;script&gt;</code>标签下载时阻塞页面解析过程，所以限制页面的<code>&lt;script&gt;</code>总数也可以改善性能。适用于内联脚本和外部脚本。</p></li><li><p><strong>非阻塞脚本</strong>：等页面完成加载后，再加载<code>js</code>代码。也就是，在<code>window.onload</code>事件发出后开始下载代码。<br> （1）<code>defer</code>属性：支持IE4和<code>fierfox3.5</code>更高版本浏览器<br> （2）动态脚本元素：文档对象模型（DOM）允许你使用js动态创建<code>HTML</code>的几乎全部文档内容。代码如下：</p><br> <script> var script=document.createElement("script"); script.type="text/javascript"; script.src="file.js"; document.getElementsByTagName("head")[0].appendChild(script); </script><p>此技术的重点在于：无论在何处启动下载，文件额下载和运行都不会阻塞其他页面处理过程。即使在head里（除了用于下载文件的http链接）。   </p></li></ul><h2 id="闭包相关问题？"><a href="#闭包相关问题？" class="headerlink" title="闭包相关问题？"></a>闭包相关问题？</h2><p>详情请见：<a href="http://segmentfault.com/blog/trigkit4/1190000000652891">详解js闭包</a></p><h2 id="js事件处理程序问题？"><a href="#js事件处理程序问题？" class="headerlink" title="js事件处理程序问题？"></a>js事件处理程序问题？</h2><p>详情请见：<a href="http://segmentfault.com/blog/trigkit4/1190000002174034">JavaScript学习总结（九）事件详解</a></p><h2 id="eval是做什么的？"><a href="#eval是做什么的？" class="headerlink" title="eval是做什么的？"></a>eval是做什么的？</h2><pre><code>它的功能是把对应的字符串解析成JS代码并运行；应该避免使用eval，不安全，非常耗性能（2次，一次解析成js语句，一次执行）。</code></pre><h2 id="写一个通用的事件侦听器函数"><a href="#写一个通用的事件侦听器函数" class="headerlink" title="写一个通用的事件侦听器函数?"></a>写一个通用的事件侦听器函数?</h2><pre><code>// event(事件)工具集，来源：github.com/markyun    markyun.Event = &#123;        // 页面加载完成后        readyEvent : function(fn) &#123;            if (fn==null) &#123;                fn=document;            &#125;            var oldonload = window.onload;            if (typeof window.onload != &#39;function&#39;) &#123;                window.onload = fn;            &#125; else &#123;                window.onload = function() &#123;                    oldonload();                    fn();                &#125;;            &#125;        &#125;,        // 视能力分别使用dom0||dom2||IE方式 来绑定事件        // 参数： 操作的元素,事件名称 ,事件处理程序        addEvent : function(element, type, handler) &#123;            if (element.addEventListener) &#123;                //事件类型、需要执行的函数、是否捕捉                element.addEventListener(type, handler, false);            &#125; else if (element.attachEvent) &#123;                element.attachEvent(&#39;on&#39; + type, function() &#123;                    handler.call(element);                &#125;);            &#125; else &#123;                element[&#39;on&#39; + type] = handler;            &#125;        &#125;,        // 移除事件        removeEvent : function(element, type, handler) &#123;            if (element.removeEnentListener) &#123;                element.removeEnentListener(type, handler, false);            &#125; else if (element.datachEvent) &#123;                element.detachEvent(&#39;on&#39; + type, handler);            &#125; else &#123;                element[&#39;on&#39; + type] = null;            &#125;        &#125;,         // 阻止事件 (主要是事件冒泡，因为IE不支持事件捕获)        stopPropagation : function(ev) &#123;            if (ev.stopPropagation) &#123;                ev.stopPropagation();            &#125; else &#123;                ev.cancelBubble = true;            &#125;        &#125;,        // 取消事件的默认行为        preventDefault : function(event) &#123;            if (event.preventDefault) &#123;                event.preventDefault();            &#125; else &#123;                event.returnValue = false;            &#125;        &#125;,        // 获取事件目标        getTarget : function(event) &#123;            return event.target || event.srcElement;        &#125;,        // 获取event对象的引用，取到事件的所有信息，确保随时能使用event；        getEvent : function(e) &#123;            var ev = e || window.event;            if (!ev) &#123;                var c = this.getEvent.caller;                while (c) &#123;                    ev = c.arguments[0];                    if (ev &amp;&amp; Event == ev.constructor) &#123;                        break;                    &#125;                    c = c.caller;                &#125;            &#125;            return ev;        &#125;    &#125;; </code></pre><h2 id="Node-js的适用场景？"><a href="#Node-js的适用场景？" class="headerlink" title="Node.js的适用场景？"></a>Node.js的适用场景？</h2><pre><code>高并发、聊天、实时消息推送   </code></pre><h2 id="JavaScript原型，原型链-有什么特点？"><a href="#JavaScript原型，原型链-有什么特点？" class="headerlink" title="JavaScript原型，原型链 ? 有什么特点？"></a>JavaScript原型，原型链 ? 有什么特点？</h2><pre><code>*  原型对象也是普通的对象，是对象一个自带隐式的 __proto__ 属性，原型也有可能有自己的原型，如果一个原型对象的原型不为null的话，我们就称之为原型链。*  原型链是由一些用来继承和共享属性的对象组成的（有限的）对象链。</code></pre><h2 id="页面重构怎么操作？"><a href="#页面重构怎么操作？" class="headerlink" title="页面重构怎么操作？"></a>页面重构怎么操作？</h2><pre><code>编写 CSS、让页面结构更合理化，提升用户体验，实现良好的页面效果和提升性能。</code></pre><h2 id="WEB应用从服务器主动推送Data到客户端有那些方式？"><a href="#WEB应用从服务器主动推送Data到客户端有那些方式？" class="headerlink" title="WEB应用从服务器主动推送Data到客户端有那些方式？"></a>WEB应用从服务器主动推送Data到客户端有那些方式？</h2><pre><code>html5 websoket    WebSocket通过Flash    XHR长时间连接    XHR Multipart Streaming    不可见的Iframe    &lt;script&gt;标签的长时间连接(可跨域)</code></pre><h2 id="事件、IE与火狐的事件机制有什么区别？-如何阻止冒泡？"><a href="#事件、IE与火狐的事件机制有什么区别？-如何阻止冒泡？" class="headerlink" title="事件、IE与火狐的事件机制有什么区别？ 如何阻止冒泡？"></a>事件、IE与火狐的事件机制有什么区别？ 如何阻止冒泡？</h2><pre><code> 1. 我们在网页中的某个操作（有的操作对应多个事件）。例如：当我们点击一个按钮就会产生一个事件。是可以被 JavaScript 侦测到的行为。   2. 事件处理机制：IE是事件冒泡、firefox同时支持两种事件模型，也就是：捕获型事件和冒泡型事件。； 3.  ev.stopPropagation();注意旧ie的方法 ev.cancelBubble = true;</code></pre><h2 id="ajax-是什么-ajax-的交互模型-同步和异步的区别-如何解决跨域问题"><a href="#ajax-是什么-ajax-的交互模型-同步和异步的区别-如何解决跨域问题" class="headerlink" title="ajax 是什么?ajax 的交互模型?同步和异步的区别?如何解决跨域问题?"></a>ajax 是什么?ajax 的交互模型?同步和异步的区别?如何解决跨域问题?</h2><p>详情请见：<a href="http://segmentfault.com/blog/trigkit4/1190000000691919">JavaScript学习总结（七）Ajax和Http状态字</a></p><pre><code>1. 通过异步模式，提升了用户体验  2. 优化了浏览器和服务器之间的传输，减少不必要的数据往返，减少了带宽占用  3. Ajax在客户端运行，承担了一部分本来由服务器承担的工作，减少了大用户量下的服务器负载。  2. Ajax的最大的特点是什么。  Ajax可以实现动态不刷新（局部刷新）  readyState属性 状态 有5个可取值： 0=未初始化 ，1=启动 2=发送，3=接收，4=完成ajax的缺点  1、ajax不支持浏览器back按钮。  2、安全问题 AJAX暴露了与服务器交互的细节。  3、对搜索引擎的支持比较弱。  4、破坏了程序的异常机制。  5、不容易调试。跨域： jsonp、 iframe、window.name、window.postMessage、服务器上设置代理页面</code></pre><h2 id="js对象的深度克隆"><a href="#js对象的深度克隆" class="headerlink" title="js对象的深度克隆"></a>js对象的深度克隆</h2><pre><code>  function clone(Obj) &#123;           var buf;           if (Obj instanceof Array) &#123;               buf = [];  //创建一个空的数组             var i = Obj.length;               while (i--) &#123;                   buf[i] = clone(Obj[i]);               &#125;               return buf;           &#125;else if (Obj instanceof Object)&#123;               buf = &#123;&#125;;  //创建一个空对象             for (var k in Obj) &#123;  //为这个对象添加新的属性                 buf[k] = clone(Obj[k]);               &#125;               return buf;           &#125;else&#123;               return Obj;           &#125;       &#125;  </code></pre><h2 id="AMD和CMD-规范的区别？"><a href="#AMD和CMD-规范的区别？" class="headerlink" title="AMD和CMD 规范的区别？"></a>AMD和CMD 规范的区别？</h2><p>详情请见：<a href="http://segmentfault.com/blog/trigkit4/1190000000733959">详解JavaScript模块化开发</a> </p><h2 id="网站重构的理解？"><a href="#网站重构的理解？" class="headerlink" title="网站重构的理解？"></a>网站重构的理解？</h2><pre><code>网站重构：在不改变外部行为的前提下，简化结构、添加可读性，而在网站前端保持一致的行为。也就是说是在不改变UI的情况下，对网站进行优化，在扩展的同时保持一致的UI。对于传统的网站来说重构通常是：表格(table)布局改为DIV+CSS使网站前端兼容于现代浏览器(针对于不合规范的CSS、如对IE6有效的)对于移动平台的优化针对于SEO进行优化深层次的网站重构应该考虑的方面减少代码间的耦合让代码保持弹性严格按规范编写代码设计可扩展的API代替旧有的框架、语言(如VB)增强用户体验通常来说对于速度的优化也包含在重构中压缩JS、CSS、image等前端资源(通常是由服务器来解决)程序的性能优化(如数据读写)采用CDN来加速资源加载对于JS DOM的优化HTTP服务器的文件缓存</code></pre><h2 id="如何获取UA？"><a href="#如何获取UA？" class="headerlink" title="如何获取UA？"></a>如何获取UA？</h2><pre><code>&lt;script&gt;     function whatBrowser() &#123;          document.Browser.Name.value=navigator.appName;          document.Browser.Version.value=navigator.appVersion;          document.Browser.Code.value=navigator.appCodeName;          document.Browser.Agent.value=navigator.userAgent;      &#125;  &lt;/script&gt;</code></pre><h2 id="js数组去重"><a href="#js数组去重" class="headerlink" title="js数组去重"></a>js数组去重</h2><p>以下是数组去重的三种方法：</p><pre><code>Array.prototype.unique1 = function () &#123;  var n = []; //一个新的临时数组  for (var i = 0; i &lt; this.length; i++) //遍历当前数组  &#123;    //如果当前数组的第i已经保存进了临时数组，那么跳过，    //否则把当前项push到临时数组里面    if (n.indexOf(this[i]) == -1) n.push(this[i]);  &#125;  return n;&#125;Array.prototype.unique2 = function()&#123;    var n = &#123;&#125;,r=[]; //n为hash表，r为临时数组    for(var i = 0; i &lt; this.length; i++) //遍历当前数组    &#123;        if (!n[this[i]]) //如果hash表中没有当前项        &#123;            n[this[i]] = true; //存入hash表            r.push(this[i]); //把当前数组的当前项push到临时数组里面        &#125;    &#125;    return r;&#125;Array.prototype.unique3 = function()&#123;    var n = [this[0]]; //结果数组    for(var i = 1; i &lt; this.length; i++) //从第二项开始遍历    &#123;        //如果当前数组的第i项在当前数组中第一次出现的位置不是i，        //那么表示第i项是重复的，忽略掉。否则存入结果数组        if (this.indexOf(this[i]) == i) n.push(this[i]);    &#125;    return n;&#125;</code></pre><h2 id="HTTP状态码"><a href="#HTTP状态码" class="headerlink" title="HTTP状态码"></a>HTTP状态码</h2><pre><code>100  Continue  继续，一般在发送post请求时，已发送了http header之后服务端将返回此信息，表示确认，之后发送具体参数信息200  OK   正常返回信息201  Created  请求成功并且服务器创建了新的资源202  Accepted  服务器已接受请求，但尚未处理301  Moved Permanently  请求的网页已永久移动到新位置。302 Found  临时性重定向。303 See Other  临时性重定向，且总是使用 GET 请求新的 URI。304  Not Modified  自从上次请求后，请求的网页未修改过。400 Bad Request  服务器无法理解请求的格式，客户端不应当尝试再次使用相同的内容发起请求。401 Unauthorized  请求未授权。403 Forbidden  禁止访问。404 Not Found  找不到如何与 URI 相匹配的资源。500 Internal Server Error  最常见的服务器端错误。503 Service Unavailable 服务器端暂时无法处理请求（可能是过载或维护）。</code></pre><h2 id="cache-control"><a href="#cache-control" class="headerlink" title="cache-control"></a>cache-control</h2><p>网页的缓存是由HTTP消息头中的<code>“Cache-control”</code>来控制的，常见的取值有<code>private、no-cache、max-age、must-revalidate</code>等，默认为<code>private</code>。</p><p><code>Expires</code> 头部字段提供一个日期和时间，响应在该日期和时间后被认为失效。允许客户端在这个时间之前不去检查（发请求），等同<code>max-age</code>的效果。但是如果同时存在，则被<code>Cache-Control</code>的<code>max-age</code>覆盖。</p><pre><code>Expires = &quot;Expires&quot; &quot;:&quot; HTTP-date</code></pre><p>例如</p><pre><code>Expires: Thu, 01 Dec 1994 16:00:00 GMT （必须是GMT格式）</code></pre><p>如果把它设置为<code>-1</code>，则表示立即过期</p><p><code>Expires</code>和<code>max-age</code>都可以用来指定文档的过期时间，但是二者有一些细微差别</p><pre><code>1.Expires在HTTP/1.0中已经定义，Cache-Control:max-age在HTTP/1.1中才有定义，为了向下兼容，仅使用max-age不够；2.Expires指定一个绝对的过期时间(GMT格式),这么做会导致至少2个问题：1)客户端和服务器时间不同步导致Expires的配置出现问题。 2）很容易在配置后忘记具体的过期时间，导致过期来临出现浪涌现象；3.max-age 指定的是从文档被访问后的存活时间，这个时间是个相对值(比如:3600s),相对的是文档第一次被请求时服务器记录的Request_time(请求时间)4.Expires指定的时间可以是相对文件的最后访问时间(Atime)或者修改时间(MTime),而max-age相对对的是文档的请求时间(Atime)如果值为no-cache,那么每次都会访问服务器。如果值为max-age,则在过期之前不会重复访问服务器。</code></pre><h2 id="js操作获取和设置cookie"><a href="#js操作获取和设置cookie" class="headerlink" title="js操作获取和设置cookie"></a>js操作获取和设置cookie</h2><pre><code>//创建cookiefunction setCookie(name, value, expires, path, domain, secure) &#123;    var cookieText = encodeURIComponent(name) + &#39;=&#39; + encodeURIComponent(value);    if (expires instanceof Date) &#123;        cookieText += &#39;; expires=&#39; + expires;    &#125;    if (path) &#123;        cookieText += &#39;; expires=&#39; + expires;    &#125;    if (domain) &#123;        cookieText += &#39;; domain=&#39; + domain;    &#125;    if (secure) &#123;        cookieText += &#39;; secure&#39;;    &#125;    document.cookie = cookieText;&#125;//获取cookiefunction getCookie(name) &#123;    var cookieName = encodeURIComponent(name) + &#39;=&#39;;    var cookieStart = document.cookie.indexOf(cookieName);    var cookieValue = null;    if (cookieStart &gt; -1) &#123;        var cookieEnd = document.cookie.indexOf(&#39;;&#39;, cookieStart);        if (cookieEnd == -1) &#123;            cookieEnd = document.cookie.length;        &#125;        cookieValue = decodeURIComponent(document.cookie.substring(cookieStart + cookieName.length, cookieEnd));    &#125;    return cookieValue;&#125;//删除cookiefunction unsetCookie(name) &#123;    document.cookie = name + &quot;= ; expires=&quot; + new Date(0);&#125;</code></pre>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>React自上而下的学习笔记-----理论篇</title>
    <link href="/2021/02/19/react-learn/"/>
    <url>/2021/02/19/react-learn/</url>
    
    <content type="html"><![CDATA[<h2 id="React"><a href="#React" class="headerlink" title="React"></a>React</h2><p>官网提出的：</p><blockquote><p>React是用JavaScript构建<strong>快速响应</strong>的大型Web应用程序的首选方式。它在Facebook和Instagram上的表现优秀。</p></blockquote><p>由此可见，React打造的就是快速响应，但是在我们日常使用APP、浏览器的时候，会因为一些场景造成快速响应被制约：</p><ul><li>CPU的瓶颈：当遇到大计算量的操作或者设备性能不足导致页面掉帧卡顿。</li><li>IO(input/output)的瓶颈: 发送网络请求以后，由于需要等待数据的返回才能进行下一步的操作导致不能快速的响应。</li></ul><h3 id="CPU"><a href="#CPU" class="headerlink" title="CPU"></a>CPU</h3><p>众所周知，主流浏览器刷新频率是60HZ，就是(1000ms / 60HZ) 就是16.6ms浏览器刷新一次。<br>但是，GUI渲染线程跟JS线程是互斥的，因此，JS脚本跟浏览器布局、绘制不能同时进行，而在每16.6ms内，需要完成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">JS脚本执行<br>样式布局<br>样式绘制<br></code></pre></td></tr></table></figure><p>所以一旦JS执行时间过长，超出了16.6ms，那么这次的刷新就没有时间执行后面的程序了。</p><p>react为了解决这个问题，在浏览器每一帧的时间中，预留一些时间（预留的时间为5s）给JS线程，而React会利用这个时间更新组件，但是如果预留的时间不能执行完JS，react会把线程的控制权还给浏览器，让其有时间渲染UI，而React就等待下一帧的时间来继续被中断的工作，这个过程就被称为时间切片。</p><p>而React中要使用这个时间切片，就需要开启Concurrent Mode</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs React">&#x2F;&#x2F; 通过使用ReactDOM.unstable_createRoot开启Concurrent Mode<br>ReactDOM.unstable_createRoot(rootEl).render(&lt;App &#x2F;&gt;);<br></code></pre></td></tr></table></figure><blockquote><p>所以综上所述，React解决CPU的短板问题，使用了时间切片，而时间切片的关键就是将同步的更新变成可以中断的异步更新。</p></blockquote><h3 id="IO"><a href="#IO" class="headerlink" title="IO"></a>IO</h3>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>前端知识点的杂记</title>
    <link href="/2021/02/18/react1/"/>
    <url>/2021/02/18/react1/</url>
    
    <content type="html"><![CDATA[<h3 id="HTML"><a href="#HTML" class="headerlink" title="HTML"></a>HTML</h3><h4 id="doctype-文档类型-的作用是什么？？？"><a href="#doctype-文档类型-的作用是什么？？？" class="headerlink" title="doctype(文档类型)的作用是什么？？？"></a>doctype(文档类型)的作用是什么？？？</h4><p>用于声明文档类型和DTD规范，确保不同的浏览器能够用相同的是方法解析文档，并且执行相同的渲染模式。<br>DTD：文档类型定义，是XML的文档类型定于，是XML的约束。</p><h4 id="浏览器标准模式-standards-mode-、接近标准模式（almost-standards-mode）和怪异模式-quirks-mode-之间的区别是什么？"><a href="#浏览器标准模式-standards-mode-、接近标准模式（almost-standards-mode）和怪异模式-quirks-mode-之间的区别是什么？" class="headerlink" title="浏览器标准模式 (standards mode) 、接近标准模式（almost standards mode）和怪异模式 (quirks mode) 之间的区别是什么？"></a>浏览器标准模式 (standards mode) 、接近标准模式（almost standards mode）和怪异模式 (quirks mode) 之间的区别是什么？</h4><p>怪异模式：向后兼容，因为老的浏览器是没有很多的规范，但随着浏览器的发展，制定了越来越多的规范。所以为了兼容这样的老是浏览器，才有了怪异模式。<br>接近标准模式：几乎跟标准模式差不多，但是还是有些差别，比方对于表格单元格中设置图像会去掉下面的空隙。标准的单元格内设置图像，由于图像会以文字的基线为准，比如想g，j这样要显示下面，就会产生空隙。<br>标准模式：以最高的标准渲染。</p><h4 id="HTML和XHTML有什么区别？"><a href="#HTML和XHTML有什么区别？" class="headerlink" title="HTML和XHTML有什么区别？"></a>HTML和XHTML有什么区别？</h4><ol><li>XHTML有大小写区分，比如<DIV>跟<div>是两个东西</li><li>XHTML有良好的文档结构，需要合理的嵌套。比如<p><span></p></span>这种是有问题的。</li><li>XHTML必须要有结束标签，即使是空标签也必须要有&lt;/&gt;，否则会报错。</li><li>XHTML必须拥有一个根标签，就是必须嵌套在<html>根元素中。</li><li>XHTML中的属性必须使用引号，否则会报错。</li><li>HTML中用脚本读取HTML标签或者属性的时候，返回的是大写；但是XHTML返回的是小写。</li></ol><h4 id="如果页面使用’application-xhtml-xml’会有什么问题吗？"><a href="#如果页面使用’application-xhtml-xml’会有什么问题吗？" class="headerlink" title="如果页面使用’application/xhtml+xml’会有什么问题吗？"></a>如果页面使用’application/xhtml+xml’会有什么问题吗？</h4><p>如果使用了XHTML，页面结构中的所有的标签都必须关闭，包括空标签。而且”application/xhtml+xml”在部分老的浏览器中不支持。</p><h3 id="CSS"><a href="#CSS" class="headerlink" title="CSS"></a>CSS</h3><h4 id="盒模型"><a href="#盒模型" class="headerlink" title="盒模型"></a>盒模型</h4><blockquote><p>目前主流的盒模型分为两类：</p><ol><li>W3C标准盒模型：content + padding + border + margin</li><li>IE盒模型: content(content + padding + border) + margin</li></ol><p>盒模型是可以通过box-sizing设置：<br>content-box (W3C标准盒模型)<br>border-box (IE盒模型)</p></blockquote><h4 id="BFC"><a href="#BFC" class="headerlink" title="BFC"></a>BFC</h4><p>BFC 称为块级格式化上下文，是一块独立的区域，使BFC内部的元素与外部的元素分隔开来。<br>一开始是为了兼容CSS1.0中发明的P标签的样式，P标签跟P标签之间的margin上下距离都是一致，呈现美观。所以在2.0里面放入了BFC，即兼容了CSS1的设计，也比较符合设计师的设计。</p><p><code>能够产生BFC的条件：</code></p><blockquote><p>根元素<br>浮动元素（不为none的时候）<br>定位元素（absolute || fixed）<br>行内块元素（display：inline-block）<br>表格单元格（display：table-cell）<br>表格标题（display: table-caption）<br>匿名表格单元格元素<br>overflow（不为visible）<br>display(不是flow-root)<br>弹性元素<br>网格元素<br>多列容器</p></blockquote><p><code>BFC解决的一些事情</code></p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs css">实现自定义的两栏布局<br>解决了浮动以后，父元素高度塌陷情况<br><span class="hljs-attribute">margin</span>的重叠<br></code></pre></td></tr></table></figure><h4 id="居中布局"><a href="#居中布局" class="headerlink" title="居中布局"></a>居中布局</h4><ol><li><p>水平居中布局</p><blockquote><p>行内元素：设置text-align: center<br>块状元素：</p><ul><li>margin：0 auto；</li><li>position: absolute + trnsform: translateX()</li><li>dispay: flex + justify-content: center</li></ul></blockquote></li><li><p>垂直居中布局</p><blockquote><p>行内元素：设置line-height：；<br>块状原色：</p><ul><li>position: absolute + transform: translateY()</li><li>display: flex + align-items: center</li></ul></blockquote></li><li><p>水平垂直居中布局</p><blockquote><p>position: absolute + transform: translate3d() // 可以在设置的同时，开启硬件加速<br>display: flex + justify-content: center + align-items: center</p></blockquote></li></ol><h4 id="display-none-跟visibility-hidden-的区别是什么？"><a href="#display-none-跟visibility-hidden-的区别是什么？" class="headerlink" title="display:none;跟visibility:hidden;的区别是什么？"></a>display:none;跟visibility:hidden;的区别是什么？</h4><ol><li><code>display:none;</code>: 隐藏元素，在布局中不会给其元素分配位置空间，会被当成不存在。</li><li><code>visibility:hidden;</code>: 隐藏元素，但是在布局存在，只是视觉上看不到而已。</li></ol><h4 id="Link跟-import的区别"><a href="#Link跟-import的区别" class="headerlink" title="Link跟@import的区别"></a>Link跟@import的区别</h4><blockquote><ol><li>link是html上的标签，而@import是css中的。</li><li>页面加载的时候，link标签会被同时加载出来，而@import是需要在页面加载完以后，才会被加载渲染。</li><li>link的样式权重高于@import的样式权重</li><li>link在浏览器中没有兼容性问题，而@import只能在IE5以上的浏览器中使用。</li></ol></blockquote><h3 id="JS"><a href="#JS" class="headerlink" title="JS"></a>JS</h3><h4 id="谈谈设计模式"><a href="#谈谈设计模式" class="headerlink" title="谈谈设计模式"></a>谈谈设计模式</h4><h4 id="var、let、const的区别是什么？"><a href="#var、let、const的区别是什么？" class="headerlink" title="var、let、const的区别是什么？"></a>var、let、const的区别是什么？</h4><blockquote><ul><li><code>var</code>是ES5的定义，而<code>let</code>、<code>const</code>是ES6提出的.</li><li><code>var</code>存在变量提升，而<code>let</code>、<code>const</code>不存在变量提升.</li><li><code>var</code>声明的变量，要么在全局作用域中，要么在局部作用域中。而<code>let</code>声明的变量只在块级作用域中有效。</li><li><code>let</code>、<code>const</code>声明变量之前就使用变量会报错，形成一个“暂时性死区”.</li><li><code>var</code>声明变量是可以重复声明，但是<code>let</code>、<code>const</code>声明过以后不允许重复声明.</li><li><code>var</code>声明的变量会被添加到window对象上，但是<code>let</code>、<code>const</code>声明的变量是不会被添加到windows对象上的。</li><li><code>var</code>跟<code>let</code>声明的变量的值都是可以更改的，但是<code>const</code>声明变量以后必须立刻初始化，赋初始值。因为一旦声明不对其初始化赋值，会报错。<br><code>const</code>赋值以后，无法进行更改。如果const的值是基本类型，如number，string等，无法修改值。但是如果是obj这种指向地址，而非地址里面的值，是可以更改地址里面的值，但是如果更改指向地址，会报错。</li></ul></blockquote><h4 id="promise-原理"><a href="#promise-原理" class="headerlink" title="promise 原理"></a>promise 原理</h4><h4 id="GET-跟-POST的区别"><a href="#GET-跟-POST的区别" class="headerlink" title="GET 跟 POST的区别"></a>GET 跟 POST的区别</h4><p>GET：</p><ul><li>一般用于信息的获取，使用URL传递参数，对所发送的信息数量也有限制，一般在2000个字符。</li></ul><p>POST：</p><ul><li>一般用于修改服务器上的资源，通过提交的表单传递值，对所发送的信息没有限制。</li></ul><h4 id="前端存储-cookie-sessionStorage-localStorage-的区别"><a href="#前端存储-cookie-sessionStorage-localStorage-的区别" class="headerlink" title="前端存储 cookie || sessionStorage || localStorage 的区别"></a>前端存储 cookie || sessionStorage || localStorage 的区别</h4><p>cookie：<br>严格来讲cokkie是跟后端有关系的，作用于服务端交互。但是cookie有大小的限制，以及个数的限制，大小不得超过4k，个数有的是20个有的是50个。有缓存时间限制，超过了缓存时间，就会被清楚掉。在HTTP请求的时候，会将cookie信息自动发送给后端。</p><p>sessionStorage:<br>本地存储的方式之一，是一种会话存储，生命周期是会话框的时间，一旦会话框被关闭，存储的信息也就被清除了。</p><p>localStorage:<br>本地存储，大小有5M，存储的数据只要不手动清除是不会消失一直存在于本地。</p><h4 id="请解释-、-和-的区别。"><a href="#请解释-、-和-的区别。" class="headerlink" title="请解释 、 和  的区别。"></a>请解释 <script>、<script async> 和 <script defer> 的区别。</h4><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs HTML"><span class="hljs-tag">&lt;<span class="hljs-name">script</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br>// 在不添加任何属性的时候,浏览器只要读到就会立即加载以及执行，不会等待后续文档加载，因此可能会阻塞文档读取。<br></code></pre></td></tr></table></figure><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs HTML"><span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">async</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br>// async是会异步加载以及执行，只要加载完毕，就会立刻执行。<br></code></pre></td></tr></table></figure><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs HTML"><span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">defer</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br>// defer跟async在加载上一样，都是异步执行的，但是不会跟async一样加载完毕就执行，它会等到后续文档都加载完毕了，再执行。因此defer会比较符合我们日常开发的需求。<br></code></pre></td></tr></table></figure><h4 id="什么是渐进式渲染-progressive-rendering-？"><a href="#什么是渐进式渲染-progressive-rendering-？" class="headerlink" title="什么是渐进式渲染 (progressive rendering)？"></a>什么是渐进式渲染 (progressive rendering)？</h4><p>渐进式渲染是为了提高网页的优化，是一种尽快呈现页面的技术。比如图片懒加载，比如分层次渲染。</p><h4 id="防抖跟节流"><a href="#防抖跟节流" class="headerlink" title="防抖跟节流"></a>防抖跟节流</h4><ol><li>防抖：<blockquote><p>思路：如果在短时间内大量的触发同一个事件操作，就重新计时。比如说，设置时间空隙为2s，用户点击了操作以后，在2s以后会触发时间，如果在2s以内不停的触发，就会重新计算时间，等用户停止操作以后的2s内才会执行操作。</p></blockquote></li></ol><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// 延时执行</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">debounce</span>(<span class="hljs-params">fn, delay</span>) </span>&#123;<br>    <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>        <span class="hljs-keyword">if</span> (timer) <span class="hljs-built_in">clearTimeout</span>(timer);<br>        timer = <span class="hljs-built_in">setTimeout</span>(fn, delay);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// 可以立即执行</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">debounce</span>(<span class="hljs-params">fn, delay = <span class="hljs-number">1000</span>, immediate = <span class="hljs-literal">true</span></span>) </span>&#123;<br>    <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>        <span class="hljs-keyword">let</span> _that = <span class="hljs-built_in">this</span>;<br>        <span class="hljs-keyword">if</span> (timer) <span class="hljs-built_in">clearTimeout</span>(timer);<br>        <span class="hljs-keyword">if</span> (immediate) &#123;<br>            <span class="hljs-keyword">if</span> (!timer) fn.apply(_that);<br>            timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>                timer = <span class="hljs-literal">null</span>;<br>            &#125;, delay);<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>                fn.apply(_that);<br>            &#125;, delay)<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ol><li>节流：<blockquote><p>思路：如果短时间内大量的触发同一件事情的操作，在指定时间内，不管触发了几次，都只工作一次。</p></blockquote></li></ol><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs javascript"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">throttle</span>(<span class="hljs-params">fn, delay</span>) </span>&#123;<br>    <span class="hljs-keyword">let</span> valid = <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>        <span class="hljs-keyword">if</span> (!valid) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        valid = <span class="hljs-literal">false</span>;<br>        <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>            fn();<br>            valid = <span class="hljs-literal">true</span>;<br>        &#125;, delay)<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="REACT"><a href="#REACT" class="headerlink" title="REACT"></a>REACT</h3><h4 id="什么是react？"><a href="#什么是react？" class="headerlink" title="什么是react？"></a>什么是react？</h4><p>react是一个用于构建用户界面的javascript库，主要关注MVC中的VIEW层面。</p><h4 id="为什么使用react？"><a href="#为什么使用react？" class="headerlink" title="为什么使用react？"></a>为什么使用react？</h4><ul><li>使用VDOM而不是真实的DOM</li><li>可以用服务端渲染</li><li>遵循单向数据流或者数据绑定</li></ul><h4 id="react的优点跟缺点"><a href="#react的优点跟缺点" class="headerlink" title="react的优点跟缺点"></a>react的优点跟缺点</h4><p>优点：</p><ul><li><p>提高应用的性能</p></li><li><p>能够在客户端跟服务端使用</p></li><li><p>使用JSX，代码可读性更高</p></li><li><p>灵活：能与其他的库跟框架集成配合</p></li><li><p>组件化</p><p>缺点：</p></li><li><p>只是一个库，不是完整的框架</p></li><li><p>库很庞大需要时间理解</p></li><li><p>对于新手不是很友好</p></li><li><p>编程时会使用到内联模块个JSX，造成编码复杂</p></li></ul><h4 id="什么是JSX？"><a href="#什么是JSX？" class="headerlink" title="什么是JSX？"></a>什么是JSX？</h4><p>JSX是JavaScript Xml的简写，是JS内定义的一套XML语法，目前要使用Babel才能解析编译JSX，而Babel转换JSX以后其实就是React中的React.createElement的调用。而使用JSX的理由能让开发人员使用类HTML的标签创建虚拟DOM进行布局，即提高了开发效率，也减低了学习成本。</p><h4 id="什么时候使用状态管理器？"><a href="#什么时候使用状态管理器？" class="headerlink" title="什么时候使用状态管理器？"></a>什么时候使用状态管理器？</h4><p>我们都知道react只是DOM的一个抽象层，并不是整个web的解决方案。如果你的UI层非常的简单，而且没有太多的互动，你完全不需要用到状态管理，否则会让你的代码变得复杂。<br>但是如果你的项目中有大量的交互跟多数据源的话：</p><ul><li>用户的使用方式复杂</li><li>多个用户身份，多个使用方式</li><li>多个用户之间可以协作</li><li>与服务器大量的进行交互</li><li>View要从多个来源获取数据<br>组件层面来看：</li><li>某个组件的状态需要被共享</li><li>某个组件的信息要在任何地方可以拿到</li><li>一个组件能够改变全局的状态</li><li>一个组件能够改变另一个没关系的组件的状态</li></ul><h4 id="render函数中return如果没有使用-会有什么问题？"><a href="#render函数中return如果没有使用-会有什么问题？" class="headerlink" title="render函数中return如果没有使用()会有什么问题？"></a>render函数中return如果没有使用()会有什么问题？</h4><ul><li><p>当return带着返回具体的信息，比如<code>&lt;div&gt;我是信息&lt;/div&gt;</code>，是紧跟着return后面的，不加上括号也是没问题的，如图所示:<br><img src="/img/react1/img_khao1.png" alt="return不加括号的结果图1"></p></li><li><p>当return不紧随其后带着具体的信息，信息在return的下一行开始，如果不加上括号，就会报错，如图所示:<br><img src="/img/react1/img_khao2.png" alt="return不加括号的结果图2"></p></li><li><p>当return null的时候，只要是如1一样紧跟其后，不加括号也没有关系，如图所示：<br><img src="/img/react1/img_khao3.png" alt="return不加括号的结果图3"><br>同理可得，如果要换行写null，就会出现类似图二的错误</p></li><li><p>当return后面没有任何值，当然包括没有null时，不管有没有带括号，都会报错，如下图：<br><img src="/img/react1/img_khao4.png" alt="return不加括号的结果图4"><br><img src="/img/react1/img_khao5.png" alt="return不加括号的结果图5"></p></li></ul><h4 id="setState"><a href="#setState" class="headerlink" title="setState"></a>setState</h4><p>setState是React中用来修改状态，更新视图的方式。</p><blockquote><ul><li>异步与同步：<br>我们都知道在React的实际开发中，在合成事件与生命周期钩子中，setState是”异步”的，我们无法在setState后直接获取state的更新数据，但是也不是说没有办法获取，可以通过setState(updateObj, callback)，通过callback回调函数进行获取最新的值。</li></ul><p>但是为什么会出现”异步”的情况呢，这里我们就要讲到React中的一个调用结构，就是事务。通过事务，可以统一的管理一个方法的开始跟结束。</p><ul><li>生命周期钩子：在生命周期中，我们能够看到更新策略的处于更新钩子之前，组件是处于事务流中，而在更新之后，组件就不在事务流中，因此会同步进行。</li><li>合成事件：React是基于<strong>事务流完成事件委托机制</strong>完成的，因此也是处于事务流中。</li></ul><p>通过上面的结论我们可以得出，只要不是处于事务流中的setState，就是同步的，在设置以后能够立刻获取新值，那么在哪些事件中是同步呢？</p><ul><li>原生事件：原生事件是浏览器本身的实现，与事务流无关。</li><li>setTimeout：放置于定时器中，就相当于位于定时器的线程中，会在其他的线程完成以后，开始进行的，因此事务流已经结束了，也呈现为同步。</li></ul></blockquote><p><code>注意：在我们实际开发中，同样会发现，多次连续调用setState，最后只会执行最后一次的setState。这是因为setState的合并，在合成事件中跟生命周期钩子中，多个连续的调用会被优化成一次。还有，当组件被销毁，还调用setState，React会报错。</code></p><h4 id="HOC（高阶组件）"><a href="#HOC（高阶组件）" class="headerlink" title="HOC（高阶组件）"></a>HOC（高阶组件）</h4>]]></content>
    
    
    
    <tags>
      
      <tag>HTML、CSS、JS、ES6、REACT、VUE</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hexo的世界</title>
    <link href="/2021/02/18/hello-hexo/"/>
    <url>/2021/02/18/hello-hexo/</url>
    
    <content type="html"><![CDATA[<h3 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h3><hr><p>其实博主是从2015年底就开始接触hexo了，但是因为当时并没有写博客的习惯，也没有记录任何信息的爱好，所有的尝试都被自己当成一次尝试就这么过去了。转眼间就到了2018年了，想着自己一事无成的博客之路，也是挺不好意思的，就把本文当成了一个真正的开始。这个努力，但是最后却以丢失hexo本地文件而最终破产！唉，索性博主跟小强一样，越挫越勇，还是打算重新来过，而这款hexo对于还没有自己服务器的小伙伴们，是比较友好的毕竟可以直接部署到github上面，如果说已经有自己服务器的同学，我还是比较建议自己动手搭建一个从零开始的自己的博客网站，毕竟作为前端的同学们，我们的vue、react、angular三大框架还是很有意思的，后端我们也是可以直接接触node，express不错！虽然目前来说有点小过时，但是对于学习技术，还是可以的。那么接下来我们还是正文开始~<br>let’s go~</p><h3 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h3><hr><p>我们在开始hexo的搭建之前，首先我们需要先分别到git跟node官网下载下来我们需要用到的git跟node。</p><p><code>我们在开始hexo的搭建之前，首先我们需要先分别到git跟node官网下载下来我们需要用到的git跟node。</code></p><h4 id="下载hexo-cli"><a href="#下载hexo-cli" class="headerlink" title="下载hexo-cli"></a>下载hexo-cli</h4><blockquote><p>npm i -g hexo-cli</p></blockquote><p>检查hexo是否下载好,如果有下列信息就说明已经有了</p><blockquote><p><code>hexo -v</code><br><img src="/img/hexo_init_v.png" alt="hexo 版本信息"></p></blockquote><p>构建hexo文件,安装依赖</p><blockquote><p>hexo init <folder><br>cd <folder><br>npm install<br><code>在这里还是要首先说明一下，如果想生成新的文件夹，可以用hexo new “My New Post”,当然看你自己的习惯，我比较建议新人可以用命令熟悉。</code></p></blockquote><p>修改hexo文件配置信息</p><blockquote><p>hexo文件中的配置皆放在_config.yml文件中，一般我们需要更改的就是：</p><ul><li>title: BlackLike （主标题）</li><li>subtitle: BlackLike’s blog （副标题）</li><li>description: This is BlackLike’s Blog, welcome to everybody. （描述信息）</li><li>author: BlackLike （作者即博主）</li></ul><p><em>以上几个为基本配置，后面还需要修改相应的配置，暂时不在此处指出，我们一步一步来</em></p></blockquote><p>本地运行</p><blockquote><p>当我们的基本配置已经完成以后，我们可能是需要先在本地查看一下效果，那么就需要我们运行一下<br><code>hexo server</code> or <code>hexo s</code></p></blockquote><h4 id="部署上github"><a href="#部署上github" class="headerlink" title="部署上github"></a>部署上github</h4><blockquote><p>本地查看了信息，感觉已经很不错了，那么我们就可以先把目前版本信息上传至github中的个人仓库中，在这里我们需要一下几步操作</p><ol><li>首先我们需要回到_config.yml文件中去，修改deploy：下面的信息，一般我们只要写：</li></ol><ul><li>type: git</li><li>repo: （远程仓库）<code>这里要注意，你的远程仓库的名称要跟你的用户名一致，比方我的是blacklike，我的仓库就是blacklike。github.io</code></li><li>branch：（分支名，一般都是master）</li></ul><ol start="2"><li>修改以后，我们需要先配置ssh，这个步骤我就不讲了，网络上有很多的博客是能写到的，所以如果配置好了ssh可以直接跳过这一步，如果没有配置的，应该是不想配置ssh的同学，请配置你的全局的git信息：</li></ol><ul><li>git config –golbal user.email “<a href="mailto:&#121;&#x6f;&#x75;&#64;&#121;&#111;&#117;&#46;&#x63;&#x6f;&#x6d;">&#121;&#x6f;&#x75;&#64;&#121;&#111;&#117;&#46;&#x63;&#x6f;&#x6d;</a>“</li><li>git config –golbal user.name “youname”</li></ul><ol start="3"><li>hexo generate (可以简写为hexo g)</li><li>hexo deploy (可以简写为 hexo d)<br>注意如果第四步出现了<code>ERROR Deployer not found: git</code>,说明你还没有为hexo安装git插件，需要运行<code>npm i hexo-deployer-git --save</code>进行安装。然后再执行第四部，然后就到你的github上面看看你的文件信息是否部署好。需要注意的是，因为hexo是部署上去的，而不是push上去的，所以只会把public里面的信息部署到上面，所以千万不要删除你本地的hexo文件，因为删除了就没有了，你如果想在git上面单独存储，可以开个仓库，单独存放。</li></ol></blockquote><h4 id="github上的配置"><a href="#github上的配置" class="headerlink" title="github上的配置"></a>github上的配置</h4><blockquote><p>文件部署完成以后，我们能看到仓库页面有个settings<br><img src="/img/setting_menu.jpg" alt="github 导航栏"><br>点击进去，设置你博客进入的入口地址<br><img src="/img/github_setting_url.jpg" alt="setting 页面"><br>这样就可以根据绿色条进入到你的博客地址当中去了！</p></blockquote><h4 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h4><ol><li><code>地址打开发现css等样式都是没有的，为什么呢？</code></li></ol><p>很多小伙伴可能在部署到github上以后打开生成好的url的时候，发现页面当中样式都是没有的，可能很疑惑，当然不需要太惊慌，可能是你的配置还没有把路径配置好<br>这时候，我们可以回到_config.yml当中，找到以下几个属性：<br>    - url: 你的url地址<br>    - root: 你的文件名，如果你没有另开文件夹，这里可以只设置-&gt; /</p><p>设置好以后，重新部署一下，就完美了，可以玩耍了~</p><h4 id="主题"><a href="#主题" class="headerlink" title="主题"></a>主题</h4><p>以上都是从零开始搭建到上传github的过程，接下来说点其他的，hexo会存在默认的主题，或许很多小伙伴都不想跟人一样，想有点自己的主题特色，那就试试，hexo的主题设置吧~</p><blockquote><p>首先，你先进入到<a href="https://hexo.io/themes/">Hexo的主题专线</a><br>然后选择你想要的喜欢的Hexo的主题<br>点击进去，一般来说都能进入到该主题的github的页面，根据作者的文档进行主题的下载跟设置</p></blockquote><h3 id="写在最后的温馨提示"><a href="#写在最后的温馨提示" class="headerlink" title="写在最后的温馨提示"></a>写在最后的温馨提示</h3><p>由于本人血的教训，已经不下几次重新创建这个hexo博客了，最头疼的就是将本地的hexo推到远程以后，远程创建的只是一个hexo编译好的架构，一旦本地的hexo文件丢失，即使把远程的文件拉下来也没有用，依旧要重新init搭建一个hexo，没有任何方式找回丢失删除的hexo本地文件，这是血的教训，请使用hexo的各位小伙伴切记，你通过hexo命令推送了一版编译好的hexo博客的同时，请做好本地hexo文件的备份跟储存，非常重要！！！！切记！！！！！</p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
